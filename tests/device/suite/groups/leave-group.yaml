appId: com.totallynotwoojin.newone
---
# Controls → "Leave this group" → tick "I understand" (the long acknowledgement
# sentence was shortened in the Sep 2026 terminology pass) → "Leave group".
- tapOn: "Conversation settings"
- extendedWaitUntil:
    visible: "Conversation controls"
    timeout: 15000
- scrollUntilVisible:
    element:
      text: "Leave this group"
    direction: DOWN
    timeout: 30000
- takeScreenshot: ${SHOT}-leave-section
- tapOn: "I understand"
# Ticking the box reveals the button below it, and the sheet is short enough
# now that it can sit under the fold; a tap there lands on the backdrop.
- scrollUntilVisible:
    element:
      text: "Leave group"
    direction: DOWN
    visibilityPercentage: 100
    timeout: 15000
- tapOn: "Leave group"
- extendedWaitUntil:
    notVisible: "Conversation controls"
    timeout: 30000
- takeScreenshot: ${SHOT}-after-leave
