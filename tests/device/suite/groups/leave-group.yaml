appId: com.totallynotwoojin.newone
---
# Controls → "Leave this group" → tick "I understand" (the long acknowledgement
# sentence was shortened in the Sep 2026 terminology pass) → "Leave group".
- tapOn: "Conversation settings"
- extendedWaitUntil:
    visible: "Conversation controls"
    timeout: 15000
- scrollUntilVisible:
    element:
      text: "Leave this group"
    direction: DOWN
    timeout: 30000
- takeScreenshot: ${SHOT}-leave-section
- tapOn: "I understand"
# Ticking the box reveals the button. The sheet fits without scrolling in
# v3.5, and scrollUntilVisible wants something scrollable, so wait for it.
- extendedWaitUntil:
    visible: "Leave group"
    timeout: 15000
- tapOn: "Leave group"
- extendedWaitUntil:
    notVisible: "Conversation controls"
    timeout: 30000
- takeScreenshot: ${SHOT}-after-leave
