appId: com.totallynotwoojin.newone
---
# Chats → Start a conversation → name → add MEMBER(s) → Create.
- launchApp
- extendedWaitUntil:
    visible: "Chats"
    timeout: 30000
- runFlow:
    when:
      notVisible: "Start a conversation"
    commands:
      - tapOn: "Chats"
- tapOn: "Start a conversation"
- extendedWaitUntil:
    visible: "Create a group"
    timeout: 20000
- tapOn:
    text: "Group name"
    index: 1
- inputText: ${NAME}
- tapOn: "Create a group"
# Typing the name leaves the keyboard up and the member rows underneath it; tap
# the GROUP SETUP eyebrow (top of the scroll view, always on screen next to the
# name field) to dismiss it before choosing members.
- tapOn: "GROUP SETUP"
- runFlow:
    when:
      visible: "(.*(Typing Predictions|Dictate).*|space)"
    commands:
      - swipe:
          direction: DOWN
          duration: 400
- takeScreenshot: ${SHOT}-details
- scrollUntilVisible:
    element:
      text: "Add ${MEMBER1}"
    direction: DOWN
    timeout: 45000
- tapOn: "Add ${MEMBER1}"
- runFlow:
    when:
      true: ${HAS_MEMBER2}
    commands:
      - scrollUntilVisible:
          element:
            text: "Add ${MEMBER2}"
          direction: DOWN
          timeout: 20000
      - tapOn: "Add ${MEMBER2}"
- takeScreenshot: ${SHOT}-members-selected
- tapOn: "Create"
- extendedWaitUntil:
    visible: ".*Write a message….*"
    timeout: 40000
- assertVisible: ${NAME}
- takeScreenshot: ${SHOT}-group-open
