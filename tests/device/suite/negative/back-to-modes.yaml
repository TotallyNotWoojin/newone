appId: com.totallynotwoojin.newone
---
- tapOn:
    text: "Use a different email or phone"
    optional: true
- extendedWaitUntil:
    visible: "Create account"
    timeout: 15000
# Consumer sign-in offers only Create account / Returning member (owner
# backlog v2): the enrollment and recovery chips must be gone.
- assertNotVisible: "First-time enrollment"
- assertNotVisible: "Recover access"
