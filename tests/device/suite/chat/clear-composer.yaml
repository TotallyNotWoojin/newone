appId: com.totallynotwoojin.newone
---
# Clear whatever draft sits in the composer, then put the keyboard away. (The
# "Original is always preserved" hint this flow used to tap is gone with the
# translation bar.)
- tapOn:
    id: "composer-input"
- eraseText: 60
- hideKeyboard
