appId: com.totallynotwoojin.newone
---
# On the other device, TEXT must disappear (delete for everyone) …
- extendedWaitUntil:
    notVisible: ${TEXT}
    timeout: ${TIMEOUT}
- takeScreenshot: ${SHOT}-gone
